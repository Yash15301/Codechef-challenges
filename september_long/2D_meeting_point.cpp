#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
// set(x,y) = (x,k+y),(x,y+k),(x+k, y+k),(x+k, y-k)

int main()
{
    int t;
    cin >> t;
    while(t--)
    {
        ll n;
        cin >> n;
        ll x[n];
        ll y[n];
        for(int i=0; i<n; i++)
        {
            cin >> x[i];
        }
        for(int i=0; i<n; i++)
        {
            cin >> y[i];
        }

    }
    return 0;
}